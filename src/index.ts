/**
 * Main entry point for the sublime-claude library
 * Exports all components that can be used both in the Chrome extension and testing
 */

// Export matcher implementations
export * from './matchers';

// Export utilities
export * from './utils';

// Export rule definitions
export * from './rules';
