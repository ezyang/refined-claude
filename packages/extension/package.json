{
  "name": "@sublime-claude/extension",
  "version": "0.1.0",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "prebuild": "mkdir -p dist && mkdir -p public/images",
    "build": "cp -f src/icon*.png public/images/ && cp -f src/icon*.svg public/images/ 2>/dev/null || true && tsup && cp -f dist/icon*.png dist/images/ 2>/dev/null || true",
    "dev": "cp -f src/icon*.png public/images/ && cp -f src/icon*.svg public/images/ 2>/dev/null || true && tsup --watch && cp -f dist/icon*.png dist/images/ 2>/dev/null || true",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": { "@types/chrome": "^0.0.260" },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "eslint": "^8.56.0",
    "tsup": "^8.0.1",
    "typescript": "^5.3.3",
    "vitest": "^1.2.2"
  }
}
