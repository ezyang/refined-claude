{
  "name": "refined-claude",
  "version": "0.1.0",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "prebuild": "mkdir -p dist && mkdir -p public/images",
    "build": "tsup",
    "dev": "tsup --watch",
    "test:ci": "pnpm run --filter rrweb-headless build && pnpm build && vitest run",
    "test": "vitest run",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@types/chrome": "^0.0.260"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "@refined-claude/rrweb-headless": "workspace:*",
    "eslint": "^8.56.0",
    "tsup": "^8.0.1",
    "typescript": "^5.3.3",
    "vitest": "^1.2.2"
  }
}
